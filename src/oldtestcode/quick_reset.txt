/* Use this code if the watchdog timer is causing the
   code uploads to fail. This empty code is quick to upload
   and removes the watchdog timer.
*/
#include <Arduino.h>

#define BAUDRATE 115200  // 9600, 19200, 38400, 57600, 115200

void setup() {
  Serial.begin(BAUDRATE);
}

void loop() {
  
}